# Written automatically during `convex deploy --cmd`.
NEXT_PUBLIC_CONVEX_URL=
RESEND_API_KEY=
RESEND_EMAIL_FROM=

# Required only for authenticated operator routes. Public /demo uses Convex only.
AUTH_DATABASE_URL=
BETTER_AUTH_SECRET=
# Used only by `pnpm seed:auth`; never exposed to browser code.
DEMO_ACCOUNT_PASSWORD=

SITE_URL=
